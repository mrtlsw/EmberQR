const fs = require('fs');
let code = fs.readFileSync('node_modules/qr-code-styling/lib/qr-code-styling.common.js', 'utf8');

// The original string has 2.5*n and 1.5*n
// We'll replace the SVG path string for _basicExtraRounded
code = code.replace(
  /`M \$\{i\} \$\{r\+2\.5\*n\}v `\+2\*n\+`a \$\{2\.5\*n\} \$\{2\.5\*n\}, 0, 0, 0, \$\{2\.5\*n\} \$\{2\.5\*n\}h `\+2\*n\+`a \$\{2\.5\*n\} \$\{2\.5\*n\}, 0, 0, 0, \$\{2\.5\*n\} \$\{2\.[0-9]*\*-n\}v `\+-2\*n\+`a \$\{2\.5\*n\} \$\{2\.5\*n\}, 0, 0, 0, \$\{2\.[0-9]*\*-n\} \$\{2\.[0-9]*\*-n\}h `\+-2\*n\+`a \$\{2\.5\*n\} \$\{2\.5\*n\}, 0, 0, 0, \$\{2\.[0-9]*\*-n\} \$\{2\.5\*n\}M \$\{i\+2\.5\*n\} \$\{r\+n\}h `\+2\*n\+`a \$\{1\.5\*n\} \$\{1\.5\*n\}, 0, 0, 1, \$\{1\.5\*n\} \$\{1\.5\*n\}v `\+2\*n\+`a \$\{1\.5\*n\} \$\{1\.5\*n\}, 0, 0, 1, \$\{1\.[0-9]*\*-n\} \$\{1\.5\*n\}h `\+-2\*n\+`a \$\{1\.5\*n\} \$\{1\.5\*n\}, 0, 0, 1, \$\{1\.[0-9]*\*-n\} \$\{1\.[0-9]*\*-n\}v `\+-2\*n\+`a \$\{1\.5\*n\} \$\{1\.5\*n\}, 0, 0, 1, \$\{1\.5\*n\} \$\{1\.[0-9]*\*-n\}`/g,
  '`M ${i} ${r+3*n}v `+1*n+`a ${3*n} ${3*n}, 0, 0, 0, ${3*n} ${3*n}h `+1*n+`a ${3*n} ${3*n}, 0, 0, 0, ${3*n} ${3*-n}v `+-1*n+`a ${3*n} ${3*n}, 0, 0, 0, ${3*-n} ${3*-n}h `+-1*n+`a ${3*n} ${3*n}, 0, 0, 0, ${3*-n} ${3*n}M ${i+3*n} ${r+n}h `+1*n+`a ${2*n} ${2*n}, 0, 0, 1, ${2*n} ${2*n}v `+1*n+`a ${2*n} ${2*n}, 0, 0, 1, ${2*-n} ${2*n}h `+-1*n+`a ${2*n} ${2*n}, 0, 0, 1, ${2*-n} ${2*-n}v `+-1*n+`a ${2*n} ${2*n}, 0, 0, 1, ${2*n} ${2*-n}`'
);

fs.writeFileSync('node_modules/qr-code-styling/lib/qr-code-styling.common.js', code);
console.log("Patched common.js");

let code2 = fs.readFileSync('node_modules/qr-code-styling/lib/qr-code-styling.js', 'utf8');
code2 = code2.replace(
  /`M \$\{i\} \$\{r\+2\.5\*n\}v `\+2\*n\+`a \$\{2\.5\*n\} \$\{2\.5\*n\}, 0, 0, 0, \$\{2\.5\*n\} \$\{2\.5\*n\}h `\+2\*n\+`a \$\{2\.5\*n\} \$\{2\.5\*n\}, 0, 0, 0, \$\{2\.5\*n\} \$\{2\.[0-9]*\*-n\}v `\+-2\*n\+`a \$\{2\.5\*n\} \$\{2\.5\*n\}, 0, 0, 0, \$\{2\.[0-9]*\*-n\} \$\{2\.[0-9]*\*-n\}h `\+-2\*n\+`a \$\{2\.5\*n\} \$\{2\.5\*n\}, 0, 0, 0, \$\{2\.[0-9]*\*-n\} \$\{2\.5\*n\}M \$\{i\+2\.5\*n\} \$\{r\+n\}h `\+2\*n\+`a \$\{1\.5\*n\} \$\{1\.5\*n\}, 0, 0, 1, \$\{1\.5\*n\} \$\{1\.5\*n\}v `\+2\*n\+`a \$\{1\.5\*n\} \$\{1\.5\*n\}, 0, 0, 1, \$\{1\.[0-9]*\*-n\} \$\{1\.5\*n\}h `\+-2\*n\+`a \$\{1\.5\*n\} \$\{1\.5\*n\}, 0, 0, 1, \$\{1\.[0-9]*\*-n\} \$\{1\.[0-9]*\*-n\}v `\+-2\*n\+`a \$\{1\.5\*n\} \$\{1\.5\*n\}, 0, 0, 1, \$\{1\.5\*n\} \$\{1\.[0-9]*\*-n\}`/g,
  '`M ${i} ${r+3*n}v `+1*n+`a ${3*n} ${3*n}, 0, 0, 0, ${3*n} ${3*n}h `+1*n+`a ${3*n} ${3*n}, 0, 0, 0, ${3*n} ${3*-n}v `+-1*n+`a ${3*n} ${3*n}, 0, 0, 0, ${3*-n} ${3*-n}h `+-1*n+`a ${3*n} ${3*n}, 0, 0, 0, ${3*-n} ${3*n}M ${i+3*n} ${r+n}h `+1*n+`a ${2*n} ${2*n}, 0, 0, 1, ${2*n} ${2*n}v `+1*n+`a ${2*n} ${2*n}, 0, 0, 1, ${2*-n} ${2*n}h `+-1*n+`a ${2*n} ${2*n}, 0, 0, 1, ${2*-n} ${2*-n}v `+-1*n+`a ${2*n} ${2*n}, 0, 0, 1, ${2*n} ${2*-n}`'
);
fs.writeFileSync('node_modules/qr-code-styling/lib/qr-code-styling.js', code2);
console.log("Patched js");
